<header class="header">
  <div class="container header-container">
    <a href="/" class="logo">
      <img src="/images/logo-gersys.svg" alt="Gersys Hair Studio" />
    </a>

    <nav class="nav">
      <a href="#services">Servicios</a>
      <a href="#gallery">Galería</a>
      <a href="#location">Ubicación</a>
    </nav>

    <a
      href="https://wa.me/50767631224?text=Hola%20Gersys%2C%20quiero%20reservar%20una%20cita."
      target="_blank"
      class="header-btn"
    >
      Reservar
    </a>
  </div>
</header>

<style>
.header{
  position:fixed !important;
  top:max(14px, env(safe-area-inset-top)) !important;
  left:0;
  width:100%;
  z-index:9999 !important;
  pointer-events:none;
  display:block !important;
}

.header-container{
  pointer-events:auto;
  position:relative;
  z-index:2147483647;
  min-height:72px;
  padding:0 14px 0 18px;
  display:flex;
  align-items:center;
  justify-content:space-between;
  border:1px solid rgba(0,0,0,.10);
  border-radius:999px;
  background:rgba(255,255,255,.92);
  backdrop-filter:blur(18px);
  -webkit-backdrop-filter:blur(18px);
  box-shadow:0 12px 40px rgba(0,0,0,.10);
}

.logo{
  display:flex;
  align-items:center;
}

.logo img{
  height:62px;
  width:auto;
  object-fit:contain;
  display:block;
}

.nav{
  display:flex;
  gap:8px;
  background:#f1f1f1;
  padding:6px;
  border-radius:999px;
}

.nav a{
  padding:10px 14px;
  border-radius:999px;
  font-size:.86rem;
  transition:.2s ease;
}

.nav a:hover{
  background:#000;
  color:#fff;
}

.header-btn{
  min-height:46px;
  padding:0 18px;
  display:flex;
  align-items:center;
  justify-content:center;
  background:#000;
  color:#fff;
  border-radius:999px;
  font-size:.9rem;
  transition:.2s ease;
}

@media(max-width:760px){
  .header{
    top:max(12px, env(safe-area-inset-top)) !important;
  }

  .header-container{
    width:94%;
    min-height:64px;
    padding:0 10px 0 16px;
  }

  .logo img{
    height:54px;
  }

  .nav{
    display:none;
  }

  .header-btn{
    min-height:42px;
    padding:0 15px;
    font-size:.84rem;
  }
}
</style>