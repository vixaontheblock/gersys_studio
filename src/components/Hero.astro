<section class="hero">
  <div class="hero-noise"></div>

  <div class="container hero-grid">
    <div class="hero-copy">
      <p class="eyebrow">BRISAS DEL GOLF • PANAMÁ</p>

      <h1 class="title hero-title">
        BEAUTY<br />
        IN BRISAS
      </h1>

      <p class="subtitle hero-subtitle">
        Balayage, color, uñas, maquillaje y cuidado capilar en un espacio premium, limpio y moderno.
      </p>

      <div class="hero-actions">
        <a
          href="https://wa.me/50767631224?text=Hola%20Gersys%2C%20quiero%20reservar%20una%20cita.%20%C2%BFMe%20pueden%20ayudar%20con%20disponibilidad%3F"
          target="_blank"
          class="btn-primary"
        >
          Reservar por WhatsApp
        </a>

        <a href="#services" class="btn-ghost">
          Ver servicios
        </a>
      </div>
    </div>

    <div class="hero-card">
      <div class="card-top">
        <span>GERSYS HAIR STUDIO</span>
        <span>2021</span>
      </div>

      <div class="circle">
        <span>G</span>
      </div>

      <div class="card-bottom">
        <strong>Color • Nails • Hair</strong>
        <p>Salón de belleza en Brisas del Golf.</p>
      </div>
    </div>
  </div>

  <div class="hero-divider">
    <span></span>
  </div>
</section>

<style>
.hero{
  min-height:100svh;
  position:relative;
  overflow:hidden;
  padding:140px 0 70px;
  background:
    radial-gradient(circle at 82% 8%, rgba(0,0,0,.08), transparent 28%),
    linear-gradient(180deg, #fff 0%, #f7f7f7 100%);
}

.hero-noise{
  position:absolute;
  inset:0;
  pointer-events:none;
  opacity:.35;
  background-image:
    linear-gradient(rgba(0,0,0,.035) 1px, transparent 1px),
    linear-gradient(90deg, rgba(0,0,0,.035) 1px, transparent 1px);
  background-size:42px 42px;
}

.hero-grid{
  position:relative;
  z-index:2;
  display:grid;
  grid-template-columns:1fr .72fr;
  gap:44px;
  align-items:center;
}

.hero-copy{
  display:flex;
  flex-direction:column;
  gap:24px;
}

.eyebrow{
  font-size:.78rem;
  letter-spacing:.18em;
}

.hero-title{
  font-size:clamp(4.2rem, 11vw, 10rem);
}

.hero-subtitle{
  max-width:560px;
  font-size:1.08rem;
}

.hero-actions{
  display:flex;
  gap:14px;
  flex-wrap:wrap;
}

.btn-primary,
.btn-ghost{
  display:inline-flex;
  align-items:center;
  justify-content:center;
  min-height:52px;
  padding:0 22px;
  border-radius:999px;
  font-size:.95rem;
  transition:.25s ease;
}

.btn-primary{
  background:#000;
  color:#fff;
}

.btn-primary:hover{
  transform:translateY(-2px);
}

.btn-ghost{
  border:1px solid #000;
  color:#000;
}

.btn-ghost:hover{
  background:#000;
  color:#fff;
}

.hero-card{
  min-height:560px;
  border:1px solid #111;
  border-radius:34px;
  padding:24px;
  background:
    linear-gradient(145deg, rgba(255,255,255,.9), rgba(230,230,230,.9));
  box-shadow:0 30px 90px rgba(0,0,0,.12);
  display:flex;
  flex-direction:column;
  justify-content:space-between;
  transition:.35s ease;
}

.hero-card:hover{
  transform:rotate(-1deg) translateY(-6px);
}

.card-top{
  display:flex;
  justify-content:space-between;
  gap:18px;
  font-size:.74rem;
  letter-spacing:.14em;
}

.circle{
  width:240px;
  height:240px;
  border-radius:50%;
  border:1px solid #000;
  display:grid;
  place-items:center;
  margin:auto;
}

.circle span{
  font-family:'Archivo Black', sans-serif;
  font-size:8rem;
}

.card-bottom strong{
  font-family:'Archivo Black', sans-serif;
  font-size:2rem;
  text-transform:uppercase;
  line-height:1;
}

.card-bottom p{
  margin-top:12px;
  color:#555;
}

.hero-divider{
  position:relative;
  z-index:3;
  width:100%;
  margin-top:72px;
  display:flex;
  justify-content:center;
}

.hero-divider span{
  width:min(92%, 1400px);
  height:1px;
  background:
    linear-gradient(
      90deg,
      transparent,
      rgba(0,0,0,.18),
      transparent
    );
}

@media(max-width:900px){
  .hero{
    padding:112px 0 54px;
  }

  .hero-grid{
    grid-template-columns:1fr;
    gap:32px;
  }

  .hero-title{
    font-size:clamp(4rem, 18vw, 6.8rem);
  }

  .hero-card{
    min-height:420px;
    border-radius:28px;
  }

  .circle{
    width:180px;
    height:180px;
  }

  .circle span{
    font-size:6rem;
  }

  .hero-actions a{
    width:100%;
  }

  .hero-divider{
    margin-top:48px;
  }
}
</style>