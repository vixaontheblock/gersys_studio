<section class="gallery" id="gallery">
  <div class="container">
    <div class="gallery-head">
      <p class="eyebrow">VISUAL MOOD</p>
      <h2 class="title">Minimal. Clean. Beauty.</h2>
    </div>

    <div class="gallery-board">
      <div class="tile tile-big">
        <span>01</span>
        <h3>Balayage</h3>
      </div>

      <div class="tile">
        <span>02</span>
        <h3>Nails</h3>
      </div>

      <div class="tile tile-dark">
        <span>03</span>
        <h3>Color</h3>
      </div>

      <div class="tile tile-wide">
        <span>04</span>
        <h3>Hair Studio</h3>
      </div>

      <div class="tile">
        <span>05</span>
        <h3>Makeup</h3>
      </div>

      <div class="tile tile-outline">
        <span>06</span>
        <h3>Brisas</h3>
      </div>
    </div>
  </div>
</section>

<style>
.gallery{
  padding:110px 0;
  background:#f7f7f7;
  overflow:hidden;
}

.gallery-head{
  margin-bottom:46px;
}

.eyebrow{
  font-size:.76rem;
  letter-spacing:.18em;
  color:#777;
  margin-bottom:18px;
}

.gallery-head h2{
  font-size:clamp(3rem, 8vw, 8rem);
  max-width:980px;
}

.gallery-board{
  display:grid;
  grid-template-columns:1.1fr .8fr .8fr;
  grid-auto-rows:230px;
  gap:14px;
}

.tile{
  position:relative;
  border:1px solid #d8d8d8;
  border-radius:30px;
  padding:24px;
  background:
    radial-gradient(circle at 80% 20%, rgba(0,0,0,.12), transparent 28%),
    #fff;
  display:flex;
  flex-direction:column;
  justify-content:space-between;
  overflow:hidden;
  transition:.28s ease;
}

.tile::before{
  content:"";
  position:absolute;
  inset:auto -20% -40% -20%;
  height:70%;
  background:linear-gradient(180deg, transparent, rgba(0,0,0,.08));
  transform:translateY(20px);
  transition:.28s ease;
}

.tile:hover{
  transform:translateY(-8px);
  border-color:#000;
  box-shadow:0 24px 70px rgba(0,0,0,.12);
}

.tile:hover::before{
  transform:translateY(0);
}

.tile span{
  position:relative;
  z-index:2;
  font-size:.8rem;
  color:#777;
}

.tile h3{
  position:relative;
  z-index:2;
  font-family:'Archivo Black', sans-serif;
  font-size:clamp(2rem, 4vw, 4.4rem);
  text-transform:uppercase;
  line-height:.9;
}

.tile-big{
  grid-row:span 2;
}

.tile-wide{
  grid-column:span 2;
}

.tile-dark{
  background:#000;
  color:#fff;
  border-color:#000;
}

.tile-dark span{
  color:#aaa;
}

.tile-outline{
  background:transparent;
  border:1px solid #000;
}

@media(max-width:900px){
  .gallery{
    padding:84px 0;
  }

  .gallery-board{
    grid-template-columns:1fr;
    grid-auto-rows:190px;
  }

  .tile,
  .tile-big,
  .tile-wide{
    grid-column:auto;
    grid-row:auto;
  }

  .tile{
    border-radius:24px;
  }

  .tile:hover{
    transform:none;
  }
}
</style>